import React, { FC } from "react";

import logo from "./media/logo.png";

const NavbarLogo: FC = () => {
  return <img src={logo} alt="WSEI application logo" className="logo" />;
};

export default NavbarLogo;
